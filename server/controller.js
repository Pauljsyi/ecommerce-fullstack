const helpers = require('../server/db/dbhelpers.js');

const controller = {
  getAll: (req, res) => {
    //get all request 
  },
  getOne: (req, res) => {
    //get one request
  },
  post: (req, res) => {
    // for credit card information for cart?
  }
}